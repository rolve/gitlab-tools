image: maven:3-openjdk-11

variables:
  MVN_REPO: $CI_PROJECT_DIR/maven-repo

cache:
  paths:
    - $MVN_REPO/

test:
  script:
    - mvn package -B -Dmaven.repo.local=$MVN_REPO
  artifacts:
    paths:
      - "target/gitlab-tools.jar"
